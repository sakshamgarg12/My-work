PORT=5000
NODE_ENV=development

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=crm_app
DB_USER=root
DB_PASSWORD=123456789
DB_FORCE_SYNC=false

# JWT Configuration
JWT_SECRET="abcedghfhi"
JWT_EXPIRE=7d

# Local dev: skip JWT on all protected routes (do not use in production)
# AUTH_DISABLED=true

# Optional: allow GET /api/auth/dev-token in production (default off)
# ALLOW_DEV_TOKEN=true

# Dev seed login (auto-created/updated on each server start in non-production)
SEED_USER_NAME="Catalyst Admin"
SEED_USER_EMAIL=admin@catalyst.local
SEED_USER_PASSWORD=Admin@123

